# by default we exclude everything from the Docker worker...
*

# ... and then we whitelist the files we need
!/crunchydb.pem
!/docker-app-start.sh
!/gmrc.cjs
!/migrations
!/package.json
!/pnpm-lock.yaml
!/src
!/tsconfig.json
